# Pinned to versions verified locally (June 2026) to support:
#   - LangGraph astream(stream_mode=["messages","updates"]) with langgraph_node metadata
#   - BedrockAgentCoreApp async-generator entrypoint → text/event-stream response
#
# These pins are exact (==) so an AgentCore redeploy reproduces the verified
# runtime. If you need to bump a version, do it deliberately, re-run the
# eval suite (evals/run_agentcore.py), and update this file.
langgraph==1.1.3
langchain-aws==1.4.1
langchain-core==1.2.22
boto3==1.43.37
fastmcp==3.1.1
python-dotenv==1.2.2
bedrock-agentcore==1.4.7
